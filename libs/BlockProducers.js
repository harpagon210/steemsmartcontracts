const DB_PLUGIN_NAME = require('../plugins/Database').PLUGIN_NAME;
const DB_PLUGIN_ACTIONS = require('../plugins/Database').PLUGIN_ACTIONS;

const NB_BLOCK_PRODUCERS = 8;

class BlockProducers {
  static rewardBlockProducers(ipc) {

  }
}

module.exports.BlockProducers = BlockProducers;
